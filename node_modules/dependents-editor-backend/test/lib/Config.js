import assert from 'assert';
import Config from '../../lib/Config';

describe('Config', function() {
  describe('when the js root is missing', function() {
    it('should default the root to the styles root');
  });
});
