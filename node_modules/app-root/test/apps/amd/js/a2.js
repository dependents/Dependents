// root
define(function (require) {

  var fs = require('fs'),
      path = require('path'),
      exec = require('child_process').exec,
      http = require('http'),

      // Makes this the root
      a = require('a');

  console.log('Booyah');

});
